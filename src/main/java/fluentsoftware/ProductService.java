package fluentsoftware;

import java.util.List;


public class ProductService {

    public Product getProduct(String sku) {
        throw new RuntimeException("This would be a real service call that we should avoid");
    }
}
